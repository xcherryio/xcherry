log:
  stdout: true
  level: info
  levelKey: "level"
apiService:
  address: 0.0.0.0:8801
  defaultPollingMaxWaitSeconds: 60
database:
  sql:
    dbExtensionName: postgres
    user: xdb
    password: xdbxdb
    databaseName: xdb
    connectAddr: 127.0.0.1:5432
asyncService:
  messageQueue:
    pulsar:
      pulsarClientOptions:
        url: pulsar://localhost:6650
      cdcTopic: public/default/debezium-postgres-topic
      defaultCDCTopicSubscription: default-shared
      